import { ApiRpc } from "./get-api-rpc";

/**
 * Resolves an address to a list of domains
 * @param rpc The RPC client to use
 * @param address The address to resolve
 */
export async function resolveDomainsForAddress(rpc: ApiRpc, address: string): Promise<string[]> {
  // TODO: Implement this function

  return []
}

