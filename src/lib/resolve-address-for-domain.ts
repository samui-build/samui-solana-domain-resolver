import { ApiRpc } from "./get-api-rpc";

/**
 * Resolves a domain to an address
 * @param rpc The RPC client to use
 * @param domain The domain to resolve
 */
export async function resolveAddressForDomain(rpc: ApiRpc, domain: string): Promise<string | null> {
  // TODO: Implement this function

  return null
}


 