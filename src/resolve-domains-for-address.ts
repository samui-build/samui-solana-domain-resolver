/**
 * Resolves an address to a list of domains
 * @param address The address to resolve
 */
export async function resolveDomainsForAddress(address: string): Promise<string[]> {
  // TODO: Implement this function

  return []
}

